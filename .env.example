# NVIDIA Agent API – copy to .env and fill in

# Required for the agent
NVIDIA_API_KEY=

# Optional: override model (default depends on USE_TOOLS)
# NVIDIA_MODEL=meta/llama-3.1-8b-instruct

# 1 = enable tools (browser, search, etc.)
USE_TOOLS=1

# 0 = show browser window
# BROWSER_HEADLESS=0

# Optional: Supabase for conversation persistence + memories + user context (use SERVICE ROLE key)
# SUPABASE_URL=https://xxxx.supabase.co
# SUPABASE_SERVICE_ROLE_KEY=eyJ...

# Optional: embeddings for recall_memory, store_memory, search_knowledge_base
# EMBEDDING_MODEL=nvidia/nv-embedqa-e5-v5
# EMBEDDING_DIM=1024

# Optional: model for post-response memory extraction (small/fast preferred)
# EXTRACTION_MODEL=nvidia/llama-3.2-3b-instruct-v1

# Knowledge base: Supabase table knowledge_chunks. Ingest from JSON: python scripts/ingest_knowledge.py examples/knowledge_chunks_example.json

# Optional: run_python timeout (seconds)
# PYTHON_TIMEOUT_SECONDS=10

# Optional: API docs
# API_TITLE=NVIDIA Agent API
# API_VERSION=0.1.0

# Server (default: localhost only)
# HOST=127.0.0.1
# PORT=8000
# Set HOST=0.0.0.0 to allow access from other machines

# CORS: allowed origins for browser requests (default: http://localhost:3000)
# CORS_ORIGINS=http://localhost:3000
# Multiple: CORS_ORIGINS=http://localhost:3000,https://myapp.com
# Allow all: CORS_ORIGINS=*
